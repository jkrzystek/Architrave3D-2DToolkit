[package]
name = "toolkit_units"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Length unit system, conversions, and formatted measurements for CAD precision"

[dependencies]
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
