[package]
name = "toolkit_volume"
version.workspace = true
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Dense 3D scalar/vector grids with trilinear sampling, gradients, and resampling"

[dependencies]
glam = { workspace = true }
serde = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
